[project]
name = "assignment-streamlit-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yuqi Xin", email = "yuqixin03@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
    "pandas>=3.0.5",
    "pytest>=9.1.1",
    "streamlit>=1.64.0",
]

[project.scripts]
assignment-streamlit-app = "assignment_streamlit_app:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
